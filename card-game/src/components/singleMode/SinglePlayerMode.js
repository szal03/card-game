import React from "react";

const SinglePlayerMode=(props)=>{
    return(
       <div>SinglePlayerMode
       <button onClick={props.backButton}>Powrót do menu</button>
       </div>
    )
}
export default SinglePlayerMode;