import React from "react";

const MultiPlayerMode=(props)=>{
    return(
        <div>MultiPlayerMode
            <button onClick={props.backButton}>Powrót do menu</button>
        </div>
    )
}
export default MultiPlayerMode;