import React from "react";

const MultiPlayerMode = (props) =>{
    return(
        <div>Multi player
            <button onClick={props.buttonFunction}>Powrót</button></div>
    )
}

export default MultiPlayerMode;